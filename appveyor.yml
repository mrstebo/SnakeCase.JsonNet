version: '{build}'
image: Visual Studio 2015
configuration: Release
pull_requests:
  do_not_increment_build_number: true

cache:
  - packages -> **\packages.config

install:
  - ps: choco install gitversion.portable -pre -y

assembly_info:
  patch: true

nuget:
  disable_publish_on_pr: true

before_build:
  - ps: nuget restore src/SnakeCase.JsonNet.sln
  - ps: gitversion /l console /output buildserver /updateAssemblyInfo /b (get-item env:APPVEYOR_REPO_BRANCH).Value

build:
  project: src/SnakeCase.JsonNet.sln
  verbosity: minimal

after_build:
  - cmd: nuget pack src\SnakeCase.JsonNet\SnakeCase.JsonNet.csproj -Version "%GitVersion_NuGetVersion%" -Prop "target=%CONFIGURATION%" -OutputDirectory src\SnakeCase.JsonNet
  - cmd: nuget pack src\SnakeCase.JsonNet_Portable\SnakeCase.JsonNet_Portable.csproj -Version "%GitVersion_NuGetVersion%" -Prop "target=%CONFIGURATION%" -OutputDirectory src\SnakeCase.JsonNet_Portable

artifacts:
  - path: '*.nupkg'
    name: NuGet

deploy:
  - provider: Environment
    name: PreRelease Nuget Feed
    on:
      branch: master
  - provider: Environment
    name: Public Nuget Feed
    on:
      appveyor_repo_tag: true
